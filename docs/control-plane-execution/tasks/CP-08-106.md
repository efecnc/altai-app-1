# CP-08-106 — cutover slice A primitives

- **Package:** 101 — Per-workspace single-writer cutover (PR 2)
- **Status:** implemented on `feat/fork-cp08-106-cutover-primitives`;
  accepted when fork CI is green
- **Depends on:** CP-08-105

Implement the discovery's slice A exactly: an advisory flock at
`<workspace>/.altai/work.db.lock` acquired inside `WorkStore::open` with a
typed `WorkspaceHeld` failure and kernel-released staleness (a crashed
holder releases with the process, so no stale-lock policy exists); a
feature-flag ledger table plus schema-version bump starting with
`control_plane_enabled`, so writer ownership becomes a recorded fact; and
the acceptance matrix covering desktop vs serve, desktop vs one-shot,
serve vs one-shot and one-shot vs one-shot — the second opener in every
pairing fails closed.
