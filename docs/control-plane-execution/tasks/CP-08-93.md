# CP-08-93 — repository readiness manifest fixture

- **Package:** 090 — Repository readiness and context-pack builder (PR 2)
- **Status:** accepted — `efecnc/altai-app-1` PR #57 (`9ccada3a`)

`RepositoryReadinessManifest` is transport-free and read-only. It requires an
explicit repository permit, measures bounded context bytes, and accepts only
same-scope immutable Evidence identifiers; denied scope, invalid measurement,
foreign/duplicate evidence and path or credential-like references fail closed.

**Evidence:** 270 control-plane tests and clippy passed; PR #57 passed fork CI.
