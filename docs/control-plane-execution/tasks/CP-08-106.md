# CP-08-106 — cutover slice A primitives

- **Package:** 101 — Per-workspace single-writer cutover (PR 2)
- **Status:** accepted — `efecnc/altai-app-1` PR #82
- **Depends on:** CP-08-105

Implement the discovery's slice A exactly: an advisory flock at
`<workspace>/.altai/work.db.lock` acquired inside `WorkStore::open` with a
typed `WorkspaceHeld` failure and kernel-released staleness (a crashed
holder releases with the process, so no stale-lock policy exists); a
feature-flag ledger table plus schema-version bump starting with
`control_plane_enabled`, so writer ownership becomes a recorded fact; and
the acceptance matrix covering desktop vs serve, desktop vs one-shot,
serve vs one-shot and one-shot vs one-shot — the second opener in every
pairing fails closed.

**Evidence:** 102 altai-core tests (5 lock tests incl. a two-process
spawn/kill-release verification), 313 control-plane tests (flag ledger),
1129 root-package tests; warnings-denied clippy clean; full fork CI passed
all 10 checks on both the base implementation and the review-fix commit.
Independent adversarial review returned BLOCK with one HIGH — desktop
commands open/close stores per command, so overlapping async reconcile
ticks collided into transient `WorkspaceHeld`; resolved in-scope with a
race-safe process-level cached `Arc<WorkStore>` (map guard held across
migrate+open+insert, failures uncached), plus a distinct `-32005`
held-workspace RPC code and an `INSERT OR IGNORE` migration-checkpoint
fix. Desktop now holds its workspace's single-writer lock for the app run
by design; external CLI processes fail typed-closed while it runs.
